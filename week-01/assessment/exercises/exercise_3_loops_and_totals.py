# exercise_3_loops_and_totals.py
# ---------------------------------------------------------
# SECTION 1, EXERCISE 3 -- Weekly Expense Tracker (Lite)  (MANDATORY)
# Concept: basic loop execution and running totals
#
# Full problem description and expected terminal interaction are in
# ../README.md.
# ---------------------------------------------------------
#
# PROBLEM:
#   1. Loop EXACTLY 7 times (one for each day of the week).
#   2. Each time, ask the user how much they spent that day.
#   3. Keep a running total across all 7 days.
#   4. After the loop ends, print the total spent for the week AND the
#      average daily spend (total / 7).
#
# ABSOLUTE BEGINNER TIP:
#   Your running-total variable must be created and set to 0 BEFORE the
#   loop starts. If you create it (or reset it) inside the loop body,
#   it gets wiped back to 0 on every single pass and never actually
#   accumulates anything.

# TODO: create a running total variable, set to 0, BEFORE the loop

# TODO: loop exactly 7 times using range()

    # TODO (inside the loop): ask for that day's spending, cast to a number,
    #                          and add it to the running total

# TODO: after the loop, print the total and the average (total / 7)
