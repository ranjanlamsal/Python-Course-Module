# hello_world.py
# ---------------------------------------------------------
# Module 1 - Local Environment Setup & Isolation
# Your very first Python script.
#
# HOW TO RUN THIS FILE:
#   1. Open a terminal in this folder (or navigate to it with `cd`).
#   2. Make sure your virtual environment is active -- you should see
#      "(.venv)" at the start of your terminal prompt.
#   3. Type:  python hello_world.py   (or `python3` on some systems)
# ---------------------------------------------------------

# This is a comment -- Python ignores everything after a '#' on a line.
# Comments exist purely to explain code to humans reading it later;
# they do not affect what the program does in any way.

print("Hello, MIT Kathmandu!")   # print() displays text in the terminal
print("Today we start Python.")  # each print() call outputs on its own new line

# Try this yourself: add a third print() line below with your own name in it,
# save the file, and run it again. Notice you have to SAVE before re-running --
# Python always reads the file fresh from disk each time you run it.
